server:
  port: ${AR_PORT:8083}

spring:
  application:
    name: ar-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://discovery-server:8761/eureka}

management:
  endpoints:
    web:
      exposure:
        include: health,info
