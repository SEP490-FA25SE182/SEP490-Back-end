name: Deploy to Production

on:
  push:
    branches:
      - deploy/production
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build & Push All Service Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Build & push chỉ các services cần thiết cho production (không build ai-service)
      - name: Build and push production services
        run: |
          export IMAGE_PREFIX=${{ secrets.DOCKERHUB_USERNAME }}
          export IMAGE_TAG=latest

          # Build từng service cần thiết
          docker compose build discovery-server
          docker compose build rookie-service
          docker compose build ar-service
          docker compose build api-gateway

          # Push các services cần thiết
          docker compose push discovery-server
          docker compose push rookie-service
          docker compose push ar-service
          docker compose push api-gateway

  deploy:
    name: Deploy to Production Server
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Add SSH Key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY_PRODUCTION }}

      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H 103.9.78.87 >> ~/.ssh/known_hosts

      - name: Pull and restart containers
        run: |
          ssh root@103.9.78.87 << 'EOF'
          set -e
          cd ~/SEP
          export IMAGE_PREFIX=${{ secrets.DOCKERHUB_USERNAME }}
          export IMAGE_TAG=latest

          echo "${{ secrets.SSH_PASS_PRODUCTION }}" | sudo -S docker compose pull discovery-server rookie-service ar-service api-gateway

          echo "${{ secrets.SSH_PASS_PRODUCTION }}" | sudo -S docker compose up -d
          EOF
